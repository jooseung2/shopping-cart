import React from "react";
import "rbx/index.css";
import { Card, Button, Title } from "rbx";

const Cart = () => {
  return (
    <Card>
      <Card.Header>
        <Title>Cart</Title>
      </Card.Header>
    </Card>
  );
};

export default Cart;
